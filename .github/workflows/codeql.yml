name: "CodeQL"
on:
  push:
  pull_request:
  schedule:
    - cron: '0 9 * * 1'
jobs:
  analyze:
    permissions:
      security-events: write
      actions: read
      contents: read
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with:
      languages: cpp
